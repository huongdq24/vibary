rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Public read access for all files is allowed.
      allow read;
      // TEMPORARILY allow all writes to unblock development.
      // WARNING: This is not secure for a production environment.
      allow write: if true;
    }
  }
}
