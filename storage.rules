rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Public read access for all files is allowed.
      allow read;
      // Write access (create, update, delete) is only allowed for authenticated users.
      allow write: if request.auth != null;
    }
  }
}
